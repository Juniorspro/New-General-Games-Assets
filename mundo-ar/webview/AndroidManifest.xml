<?xml version="1.0" encoding="utf-8"?>
<!-- Mundo AR Web: la página web/index.html adentro de un WebView. Acostado,
     pantalla completa, con permiso de cámara (y la linterna que da la página).
     No necesita ARCore: el 6DoF sale del SLAM en JavaScript (AlvaAR). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juniorspro.mundoarweb"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- three.js y AlvaAR se bajan de sus CDN la primera vez (después quedan en la caché del WebView). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- navigator.vibrate() en el WebView. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:label="@string/nombre"
        android:icon="@drawable/icono"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <activity
            android:name=".Principal"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|navigation|uiMode"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
